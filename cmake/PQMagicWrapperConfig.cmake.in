@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
# find_dependency(PkgConfig REQUIRED) # Uncomment if needed

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/PQMagicWrapperTargets.cmake")

# Set variables
set(PQMagicWrapper_VERSION "@PROJECT_VERSION@")
set(PQMagicWrapper_FOUND TRUE)

# Check if all components are found
check_required_components(PQMagicWrapper)